feat: route POI marker system + backend path planning API + AMap config

New:
- all-route-pois.js: coordinate database for 34 routes / 87 POIs, AMap-verified
- RouteDbProxyController: graceful degradation (200 with empty list instead of 503)

Enhanced:
- route-editor.html: prominent labeled markers (colored dot + name), auto waypoint marking, plan/edit/re-plan buttons, visible debug panel
- app.js addAllRouteOverlays: auto-display all route POI markers on map load (~90 deduped), zoom-adaptive show/hide
- Path planning switched to backend REST API (/api/custom-route/plan), bypassing AMap JS SDK VM errors
- Walking mode segments waypoints sequentially (v3 API limitation)

Fixed:
- Triple coordinate validation (null + isFinite + Nanjing bounds) to prevent Pixel(NaN) error
- Enhanced clearRouteOverlays/closeMapEditor cleanup to prevent nav bar freezing
- Default transport mode changed to walking
- Unified AMap Web JS API key with correct securityJsCode configuration
